<Window x:Class="VRCTimeline.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="VRC Timeline"
        Width="1200" Height="800"
        MinWidth="600" MinHeight="400"
        WindowStartupLocation="CenterScreen"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        Background="{DynamicResource MaterialDesignPaper}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="14"
        FontFamily="{materialDesign:MaterialDesignFont}">

    <materialDesign:DialogHost Identifier="RootDialogHost" CloseOnClickAway="False">
    <Grid>
    <DockPanel>
        <!-- Main Content -->
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="160" MinWidth="57" MaxWidth="220" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!-- Sidebar Navigation -->
            <Border Grid.Column="0" Background="{DynamicResource MaterialDesignCardBackground}"
                    BorderBrush="{DynamicResource MaterialDesignDivider}" BorderThickness="0,0,1,0">
                <DockPanel>
                    <!-- VRChat Status Indicator -->
                    <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal"
                                Margin="22,5,22,12">
                        <Ellipse Width="10" Height="10" Margin="0,0,8,0" VerticalAlignment="Center">
                            <Ellipse.Style>
                                <Style TargetType="Ellipse">
                                    <Setter Property="Fill" Value="#EF5350" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsVRChatRunning}" Value="True">
                                            <Setter Property="Fill" Value="#66BB6A" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Ellipse.Style>
                        </Ellipse>
                        <TextBlock VerticalAlignment="Center" FontSize="13">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Text" Value="{DynamicResource VRChat_NotDetected}" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsVRChatRunning}" Value="True">
                                            <Setter Property="Text" Value="{DynamicResource VRChat_Running}" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </StackPanel>

                    <ListBox SelectedIndex="{Binding SelectedNavIndex}" Margin="0,8,0,0"
                             Background="Transparent" BorderThickness="0">
                        <ListBoxItem Padding="16,12">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="Pulse" Width="24" Height="24"
                                                          Margin="0,0,14,0" VerticalAlignment="Center" />
                                <TextBlock Text="{DynamicResource Nav_Realtime}" VerticalAlignment="Center" FontSize="14" />
                            </StackPanel>
                        </ListBoxItem>
                        <ListBoxItem Padding="16,12">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="History" Width="24" Height="24"
                                                          Margin="0,0,14,0" VerticalAlignment="Center" />
                                <TextBlock Text="{DynamicResource Nav_Activity}" VerticalAlignment="Center" FontSize="14" />
                            </StackPanel>
                        </ListBoxItem>
                        <ListBoxItem Padding="16,12">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="Image" Width="24" Height="24"
                                                          Margin="0,0,14,0" VerticalAlignment="Center" />
                                <TextBlock Text="{DynamicResource Nav_Photos}" VerticalAlignment="Center" FontSize="14" />
                            </StackPanel>
                        </ListBoxItem>
                        <ListBoxItem Padding="16,12">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="Bell" Width="24" Height="24"
                                                          Margin="0,0,14,0" VerticalAlignment="Center" />
                                <TextBlock Text="{DynamicResource Nav_Notifications}" VerticalAlignment="Center" FontSize="14" />
                            </StackPanel>
                        </ListBoxItem>
                        <ListBoxItem Padding="16,12">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="Play" Width="24" Height="24"
                                                          Margin="0,0,14,0" VerticalAlignment="Center" />
                                <TextBlock Text="{DynamicResource Nav_Videos}" VerticalAlignment="Center" FontSize="14" />
                            </StackPanel>
                        </ListBoxItem>
                        <ListBoxItem Padding="16,12">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="Cog" Width="24" Height="24"
                                                          Margin="0,0,14,0" VerticalAlignment="Center" />
                                <TextBlock Text="{DynamicResource Nav_Settings}" VerticalAlignment="Center" FontSize="14" />
                            </StackPanel>
                        </ListBoxItem>
                    </ListBox>
                </DockPanel>
            </Border>

            <!-- Splitter -->
            <GridSplitter Grid.Column="1" Width="4" HorizontalAlignment="Center"
                          VerticalAlignment="Stretch" Background="Transparent"
                          ShowsPreview="False" />

            <!-- Content Area -->
            <ContentControl Grid.Column="2" Content="{Binding CurrentViewModel}" Margin="16" />
        </Grid>
    </DockPanel>

    <!-- Loading Overlay -->
    <Border Background="#88000000" Panel.ZIndex="100">
        <Border.Style>
            <Style TargetType="Border">
                <Setter Property="Visibility" Value="Hidden" />
                <Style.Triggers>
                    <DataTrigger Binding="{Binding Loading.IsLoading}" Value="True">
                        <Setter Property="Visibility" Value="Visible" />
                    </DataTrigger>
                </Style.Triggers>
            </Style>
        </Border.Style>
        <materialDesign:Card Padding="40,32" HorizontalAlignment="Center" VerticalAlignment="Center"
                              materialDesign:ElevationAssist.Elevation="Dp8"
                              UniformCornerRadius="12">
            <StackPanel HorizontalAlignment="Center">
                <ProgressBar Style="{StaticResource MaterialDesignCircularProgressBar}"
                             IsIndeterminate="True"
                             Width="48" Height="48"
                             HorizontalAlignment="Center" Margin="0,0,0,16" />
                <TextBlock Text="{Binding Loading.LoadingMessage}"
                           Style="{StaticResource MaterialDesignSubtitle1TextBlock}"
                           HorizontalAlignment="Center" />
            </StackPanel>
        </materialDesign:Card>
    </Border>
    </Grid>
    </materialDesign:DialogHost>
</Window>
